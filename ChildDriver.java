import java.util.Scanner;
class ChildDriver
{
	//Child c = new Child();
	public static void main(String[] args)
	{
		Child c = new Child();
		c.Display();
		//new Child().Display(); // can only be used once throughout the class //anonymous object
		//Child c1 = new Child(10); // no need to be created after this keyword
		//Parent p = new Parent();
		//System.out.println(c.age);
		//System.out.println(c.name);
		//System.out.println(p.toString());
	}
}